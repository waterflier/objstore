1.当data过大时，为了提高性能，允许部分写入与部分读取
2.接口修改
添加RaadbyRange,WriteByRange接口
Range范围必须在ObjData的大小以内，否则会返回错误
WriteByRange会在必要的时候触发Append语意


3.注意保障实现的原子性和正确性，
4.对象内容的任意两块的原子更新 （放到事务里去讨论）
原理
meta->pagelist->data page





结论0
通过一个可以原子更新的meta，来提供大块数据的原子读/写
通过对数据分块，提供大块数据里的部分数据的原子读/写（分页技术）
多段数据原子读写（多次操作）

作业
支持Append语义
支持数据移动语义

考虑 pagelist的压缩存储
扩展阅读： 保护模式下的内存分页技术,页表与页,MCU
smt (软件内存事务)
